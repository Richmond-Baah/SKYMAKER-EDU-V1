# .env.example

# Database (for Phase 2 backend — using Postgres with Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/drone-platform?schema=public"

# Teacher API token (optional — leave blank in development for open access)
TEACHER_TOKEN=""

# Student ID header (optional — auto-generated if not provided)
# STUDENT_ID="student_123"
